<% if entry.present? %>
    <div class="d-flex align-items-center">
        <div class="entry-favicon" style="margin-right:1rem;">
            <%= image_tag("https://www.google.com/s2/favicons?domain=#{entry.url}&sz=32", alt: entry.name) %>
        </div>
        <%= yield %>
    </div>
<% end %>